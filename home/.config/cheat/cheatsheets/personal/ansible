---
tags: [ orchestration, ansible ]
---
# run a role
ansible <host_pattern> -m include_role -a "name=<role_name>"

# see the value of a fact for a host_pattern
ansible <host_pattern> -i <inventory_path> -m setup -a "filter=<fact_name>"
